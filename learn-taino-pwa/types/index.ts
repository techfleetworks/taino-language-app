import React from "react";

// Define a custom type for your styles
export interface CustomStyles extends React.CSSProperties {
    imageContainer?: React.CSSProperties; // Add custom properties as needed
}